# 專案行為、選擇與對話紀錄 (Project Action, Decision & Q&A Log)

本文件紀錄了在開發「SVM Kernel Trick 教學專案」過程中的所有關鍵行為、設計選擇，以及開發者（User）與 AI（Antigravity）之間的所有對話與互動歷程。

---

## 💡 核心技術與設計決策 (Key Decisions)

1. **雙軌教學策略 (Dual-Track Pedagogy)**
   - 決定同時開發「Manim 動畫指令碼」與「Streamlit 互動網頁」，前者用於高品質的線性代數視覺化教學，後者用於動態展示超參數調整對模型決策曲面的影響。
2. **資料與升維設計 (Data & Explicit Lifting)**
   - 選擇 `sklearn.datasets.make_circles` 作為基礎訓練資料，因為同心圓最能展現 RBF/Poly Kernel 的非線性映射能力。
   - 為了具象化高維映射，採用簡單直觀的幾何升維函數 $z = x^2 + y^2$，讓使用者能輕易看見資料在 3D 空間中變得「線性可分」。
3. **互動式 3D 渲染技術選型 (Interactive 3D Rendering)**
   - 選擇 `Plotly` 搭配 `Streamlit`。透過 `numpy.meshgrid` 結合 SVM 的 `decision_function`，直接在網頁上繪製連續的 3D 地形曲面（Decision Landscape），而非靜態的平面圖表。

---

## 🗣️ 開發對話與行為歷程 (Chronological Discussion & Q&A)

### Round 1: 專案初始化與規劃
- **User**: 「請幫我讀取 `claude.md`, `design.md` 幫我生成專案」
- **AI**: 分析了需求文件，提出了涵蓋「核心邏輯」、「Manim 動畫」與「Streamlit 網頁」的雙軌開發 Implementation Plan，並等待使用者確認。
- **User**: 「我已存檔」(確認並批准計畫)
- **AI**: 開始動工，實作了共用的核心機器學習演算法 `svm_kernel_demo.py`、整理了環境依賴 `requirements.txt`，並完成了 README 的基礎教學架構。

### Round 2: 網站實作與啟動
- **User**: 「請幫忙我生成一個 local 網站顯示這些結果」
- **AI**: 實作了完整的 `streamlit_app.py`，加入側邊欄參數調整與 Plotly 3D 互動繪圖分頁。
- **User**: 「請幫我啟動網頁」
- **AI**: 於背景安裝缺少的套件 (`plotly`) 並以 `streamlit run streamlit_app.py` 啟動了本機網頁伺服器，提示使用者可前往 `http://localhost:8501/`。

### Round 3: 自動化驗證與品質測試
- **User**: 「http://localhost:8501/ 請幫忙驗證此功能」
- **AI**: 呼叫了內建的 Browser Subagent 自動開啟網頁進行 UI 與互動測試。代理程式成功點擊了各個分頁，修改了 SVM Kernel 為 `poly`，並確認了 3D 決策曲面的渲染皆運作完美。隨後將驗證結果與截圖匯入至 `walkthrough.md` 中。

### Round 4: 歷程紀錄與摘要彙整
- **User**: 「請幫我生成所有行為與選擇紀錄為 .\resources\log.md」
- **AI**: 梳理了開發過程中的技術選擇與架構邏輯，產出了第一版的 `log.md` 紀錄檔。
- **User**: 「請幫忙重新摘要記錄到 README.md」
- **AI**: 將日誌的精華決策摘要濃縮，附加到了 `README.md` 的最底部，並加上連結指向這份完整日誌。

### Round 5: 流程圖繪製與版本控制
- **User**: 「請生成 `workflow.png` 根據整體流程紀錄」
- **AI**: 撰寫 Python 腳本（基於 Plotly Graph Objects），動態生成了一張包含需求分析、核心邏輯、雙軌教學與自動化測試四大區塊的精美架構流程圖，輸出為 `workflow.png`。
- **User**: 「請將這些內容重新發佈到 github 上」
- **AI**: 在本地執行了 `git init`，建立了 `.gitignore` 以排除暫存檔，並將所有開發成果（共 9 個檔案）打包建立了 Initial Commit。並向使用者提供了後續 Push 到遠端 GitHub 的終端機指令。

### Round 6: 最終歷程大總結
- **User**: 「summarize everything and write to log.md, including all discussion and Q&A」
- **AI**: 將過去所有的技術決策與上述 1~6 回合的對話及問答互動歷程完整彙整，全面改寫並擴充了本份 `log.md` 文件。

---
*Document automatically generated by Antigravity AI on 2026-06-18.*
